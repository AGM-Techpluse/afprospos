export default function Dashboard() {
    return (
        <div>
            <h1>Admin dashboard</h1>
            <p>
                Reaching this page proves: staff auth guard, active-account
                check, and shop-context resolution all passed. Phase 2
                replaces this with the real shared-design-system shell.
            </p>
        </div>
    );
}
